import 'package:flutter/material.dart';

class losspg extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text('削減量'),
      ),
      body: Center(
        child: Text('削減しろ'),
      ),
    );
  }
}